{
  "name": "task-board",
  "version": "1.0.0",
  "description": "A minimal full-stack task board starter app (Express API + vanilla frontend).",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "4.21.2"
  }
}
