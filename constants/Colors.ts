import { palette } from '@/lib/theme';

export default {
  light: {
    text: palette.paper,
    background: palette.void,
    tint: palette.pulse,
    tabIconDefault: palette.mist,
    tabIconSelected: palette.pulse,
  },
  dark: {
    text: palette.paper,
    background: palette.void,
    tint: palette.pulse,
    tabIconDefault: palette.mist,
    tabIconSelected: palette.pulse,
  },
};